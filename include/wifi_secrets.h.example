#pragma once

// Copie este arquivo para wifi_secrets.h e preencha para conectar na sua rede
// na primeira gravacao (opcional — tambem pode configurar pela interface web).

#define WIFI_STA_SSID ""
#define WIFI_STA_PASSWORD ""
